const AllQuestions = () => {

    // TODO: design + functionality
    // (simply import all questions? doesn't need to know where user is at)

    return (
        <div>
            <p className="flex mb-64 justify-center">Alle Fragen :-)</p>
        </div>
    )
}

export default AllQuestions;