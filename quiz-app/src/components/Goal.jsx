const Goal = () => {

    /**  
     * TODO: add design
     * either: feedback page and THEN goal (sum up of complete performance?)
     * or: combine (like in prototype)
     * */

    return (
        <div>
            <p className="flex mb-64 justify-center">Goal :-)</p>
        </div>
    )
}

export default Goal;